<UserControl x:Class="POS.Desktop.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="440" Style="{StaticResource CardStyle}" Padding="32">
            <StackPanel>
                <TextBlock Text="Change Password" Style="{StaticResource HeroTitleStyle}" FontSize="22" Margin="0,0,0,6"/>
                <TextBlock Text="For security reasons, please update your account password." Style="{StaticResource MutedCaptionStyle}" Margin="0,0,0,24"/>

                <!-- Error Message -->
                <Border Background="{StaticResource DangerLightBrush}" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,16"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVis}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeSmall}" />
                </Border>

                <!-- Success Message -->
                <Border Background="{StaticResource SuccessLightBrush}" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,16"
                        Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVis}}">
                    <TextBlock Text="{Binding SuccessMessage}" Foreground="{StaticResource SuccessBrush}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeSmall}" />
                </Border>

                <TextBlock Text="Current / Old Password *" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding OldPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,14"/>

                <TextBlock Text="New Password *" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,14"/>

                <TextBlock Text="Confirm New Password *" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ConfirmNewPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,24"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Skip / Cancel" Command="{Binding CancelCommand}" Style="{StaticResource OutlinedButtonStyle}" Height="42" />
                    <Button Grid.Column="2" Content="Update Password" Command="{Binding ChangePasswordCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="42" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
