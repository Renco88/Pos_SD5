<UserControl x:Class="POS.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="{StaticResource WindowBgBrush}">
        <!-- Login Card Center -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="420"
                Style="{StaticResource CardStyle}"
                Padding="36">
            <StackPanel>
                <!-- Brand Logo / Title -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                    <Path Data="{StaticResource IconCart}" Fill="{StaticResource PrimaryBrush}" Width="28" Height="28" Stretch="Uniform" Margin="0,0,10,0"/>
                    <TextBlock Text="NexPOS" Style="{StaticResource HeroTitleStyle}" Foreground="{StaticResource PrimaryDarkBrush}" />
                </StackPanel>

                <TextBlock Text="Professional Point of Sale System" Style="{StaticResource MutedCaptionStyle}" HorizontalAlignment="Center" Margin="0,0,0,28" />

                <!-- Error Message -->
                <Border Background="{StaticResource DangerLightBrush}" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,16"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVis}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeSmall}" />
                </Border>

                <!-- Username -->
                <TextBlock Text="Username or Email" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,6" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16" />

                <!-- Password -->
                <TextBlock Text="Password" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,6" />
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,24" />

                <!-- Login Button -->
                <Button Content="Sign In to POS" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="44" FontSize="{StaticResource FontSizeSubheader}" Margin="0,0,0,20" />

                <Separator Background="{StaticResource BorderBrush}" Margin="0,0,0,16"/>

                <!-- Quick Presets -->
                <TextBlock Text="Quick Test Accounts:" Style="{StaticResource MutedCaptionStyle}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Admin (Employer)" Command="{Binding UseAdminCredentialsCommand}" Style="{StaticResource OutlinedButtonStyle}" FontSize="{StaticResource FontSizeSmall}" />
                    <Button Grid.Column="2" Content="Worker (Cashier)" Command="{Binding UseWorkerCredentialsCommand}" Style="{StaticResource OutlinedButtonStyle}" FontSize="{StaticResource FontSizeSmall}" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
