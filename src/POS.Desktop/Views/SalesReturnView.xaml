<UserControl x:Class="POS.Desktop.Views.SalesReturnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Sales Returns &amp; Refunds" Style="{StaticResource SectionTitleStyle}" VerticalAlignment="Center" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource OutlinedButtonStyle}" HorizontalAlignment="Right" />
                </Grid>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Returns}" Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Return #" Binding="{Binding ReturnNumber}" Width="140" FontWeight="SemiBold" />
                        <DataGridTextColumn Header="Original Invoice" Binding="{Binding OriginalInvoiceNumber}" Width="150" />
                        <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*" />
                        <DataGridTextColumn Header="Refund Amount" Binding="{Binding RefundAmount, StringFormat='৳{0:F2}'}" Width="120" FontWeight="Bold" />
                        <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="180" />
                        <DataGridTextColumn Header="Date" Binding="{Binding ReturnDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="150" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
