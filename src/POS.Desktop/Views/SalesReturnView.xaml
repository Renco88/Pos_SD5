<UserControl x:Class="POS.Desktop.Views.SalesReturnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid>
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Sales Returns &amp; Refunds" Style="{StaticResource SectionTitleStyle}" VerticalAlignment="Center" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource OutlinedButtonStyle}" HorizontalAlignment="Right" />
                </Grid>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Returns, FallbackValue={x:Null}}" Style="{StaticResource ModernDataGridStyle}" AutoGenerateColumns="False" IsReadOnly="True" CanUserResizeColumns="True" RowBackground="Transparent" AlternatingRowBackground="#FAFAFA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Return Invoice #" Binding="{Binding ReturnInvoiceNumber, TargetNullValue='N/A', FallbackValue='N/A'}" Width="160" FontWeight="SemiBold" />
                        <DataGridTextColumn Header="Original Invoice" Binding="{Binding OriginalInvoiceNumber, TargetNullValue='N/A', FallbackValue='N/A'}" Width="150" />
                        <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName, TargetNullValue='Walk-in', FallbackValue='N/A'}" Width="*" />
                        <DataGridTextColumn Header="Cashier" Binding="{Binding CashierName, TargetNullValue='N/A', FallbackValue='N/A'}" Width="140" />
                        <DataGridTextColumn Header="Total Refund" Binding="{Binding TotalRefundAmount, StringFormat='৳{0:F2}', TargetNullValue='৳0.00', FallbackValue='৳0.00'}" Width="125" FontWeight="Bold" Foreground="#DC2626" />
                        <DataGridTextColumn Header="Adjusted From Due (৳)" Binding="{Binding AdjustedFromCustomerDue, StringFormat='৳{0:F2}', TargetNullValue='৳0.00', FallbackValue='৳0.00'}" Width="160" />
                        <DataGridTextColumn Header="Reason" Binding="{Binding ReturnReason, TargetNullValue='N/A', FallbackValue='N/A'}" Width="220" />
                        <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm', TargetNullValue='', FallbackValue=''}" Width="150" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
