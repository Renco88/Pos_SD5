<UserControl x:Class="POS.Desktop.Views.ExpenseManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Expenses &amp; Operating Costs" Style="{StaticResource SectionTitleStyle}" VerticalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Record Expense" Command="{Binding OpenCreateModalCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource OutlinedButtonStyle}" />
                    </StackPanel>
                </Grid>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Expenses}" Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" FontWeight="SemiBold" />
                        <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="160" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='৳{0:F2}'}" Width="120" FontWeight="Bold" />
                        <DataGridTextColumn Header="Payment" Binding="{Binding PaymentMethod}" Width="120" />
                        <DataGridTextColumn Header="Date" Binding="{Binding ExpenseDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="160" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- CREATE MODAL -->
        <Grid Background="#80000000" Visibility="{Binding IsModalOpen, Converter={StaticResource BoolToVis}}">
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Style="{StaticResource CardStyle}" Padding="24">
                <StackPanel>
                    <TextBlock Text="Record Expense" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,16"/>

                    <TextBlock Text="Description *" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,12"/>

                    <TextBlock Text="Amount (৳) *" Style="{StaticResource SubheaderStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Cancel" Command="{Binding CloseModalCommand}" Style="{StaticResource OutlinedButtonStyle}" Height="38" />
                        <Button Grid.Column="2" Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="38" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
